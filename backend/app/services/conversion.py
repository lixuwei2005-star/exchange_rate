from __future__ import annotations

# Fully implemented in Phase 9. Constants below are placeholders — see §6 of CLAUDE.md
# for the canonical values; we'll move them in once tests exercise them.


def apply_fees(*args, **kwargs):  # type: ignore[no-untyped-def]
    raise NotImplementedError("Fee conversion is implemented in Phase 9.")
